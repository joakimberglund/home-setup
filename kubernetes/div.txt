# /etc/rancher/k3s/registries.yaml
#
# No need for TLS/HTTPS for the local registry
# https://docs.k3s.io/installation/private-registry
#
mirrors:
 docker.io:
    endpoint:
      - "http://registry.cube.local:5000"
  registryi.cube.local:
    endpoint:
      - "http://registry.cube.local:5000"

mirrors:
  "*":
    endpoint:
      - "http://registry.cube.local:5000"
configs:
  "docker.io":
  "*":
    tls:
      insecure_skip_verify: true

