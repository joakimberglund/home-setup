apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: omada
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/joakimberglund/home-setup.git'
    targetRevision: main
    path: "kubernetes/omada"
  destination:
    server: https://kubernetes.default.svc
    namespace: omada
  syncPolicy:
    automated: {}
    syncOptions:
    - CreateNamespace=true
